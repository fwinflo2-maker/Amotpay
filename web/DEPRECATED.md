# Deprecated — not the AMOTPay user application

The `web/` folder was an early Sumsub Web SDK experiment.

**AMOTPay user experience = `mobile/` (Expo / React Native) only.**

Admin operations = `admin/` (React / Vite).

Do not deploy or extend `web/` as a user-facing product. New UX work belongs in `mobile/`.
